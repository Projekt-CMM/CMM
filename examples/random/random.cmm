#include <stdio.h>
#include <stdlib.h>

void main() {
	// init random number generator
	srand(time());

	// print 10 random numbers in the range from 1-10
	int i = 0;
	for(; i < 10; i++)
		printf("%d ", rand() % 10 + 1); 
}